spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/oanabogos/configuration.git
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    secret: guest
server:
  port: 8888
security:
  user:
    name: root
    password: s3cr3t
